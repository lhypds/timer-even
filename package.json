{
  "name": "timer-even",
  "version": "0.1.2",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node --experimental-strip-types --test tests/*.test.mjs",
    "version": "node -e \"const fs = require('fs'); const app = JSON.parse(fs.readFileSync('app.json', 'utf8')); app.version = require('./package.json').version; fs.writeFileSync('app.json', JSON.stringify(app, null, 2) + '\\n')\" && prettier --write app.json && git add app.json",
    "even:pack": "bash ./package.sh",
    "simulate": "evenhub-simulator http://localhost:5173"
  },
  "dependencies": {
    "@evenrealities/even_hub_sdk": "0.0.14"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~6.0.2",
    "vite": "^8.0.13"
  }
}
